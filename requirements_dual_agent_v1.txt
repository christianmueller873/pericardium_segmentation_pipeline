# API-only additions to the existing SAROS/Agent-2 Poetry environment.
# Torch, MONAI, nnU-Net, NumPy, SciPy, and nibabel are already installed.
fastapi>=0.110,<1
uvicorn>=0.30,<1
python-multipart>=0.0.9,<1
