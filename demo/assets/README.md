# Demo media

The reviewed real-inference recording is stored in this directory as:

`full_pipeline_demo.mp4`

It uses the approved public SAROS CT and was reviewed for personal paths,
notifications, and visible identifiers. The public copy is 74.6 seconds,
1600×772 H.264 at 24 fps, has no audio, and is 1,232,200 bytes.

SHA-256:
`9ac53ddbfecc1b59fb3fbad543e736cf7fb03d69a198c776c79791ca0b0b60d6`

The idle inference wait was replaced by an explicit disclosure card. The
post-inference portion is shown at 2× speed. The untouched source recording is
preserved locally under the ignored `release_assets/source_recordings/`
directory and is not part of the public repository.
